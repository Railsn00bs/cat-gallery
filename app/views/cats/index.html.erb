<h1>All the cats!</h1>

<% Cat.all.each do |cat| %>
  <img src='<%= cat.image_url %>'/>
<% end %>

<%= form_for Cat.new do |f| %>
  <%= f.label :image_url, 'Cat Url!' %>
  <%= f.text_field :image_url %>

  <%= f.submit %>
<% end %>
